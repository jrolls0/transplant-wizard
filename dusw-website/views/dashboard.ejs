<%- include('partials/header', {title: title}) %>

<!-- Main Dashboard Container -->
<div class="container-fluid px-4 py-4">
    <!-- Welcome Hero Section -->
    <div class="row mb-5">
        <div class="col-12">
            <div class="card border-0" style="background: var(--primary-gradient); color: white; overflow: hidden;">
                <div class="card-body p-5 position-relative">
                    <div class="row align-items-center">
                        <div class="col-lg-8">
                            <div class="mb-3">
                                <span class="badge bg-white bg-opacity-90 text-dark px-3 py-2 rounded-pill">
                                    <i class="fas fa-calendar-day me-2"></i>
                                    <%= new Date().toLocaleDateString('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %>
                                </span>
                            </div>
                            <h1 class="display-6 fw-bold mb-3">
                                Good <%= new Date().getHours() < 12 ? 'morning' : new Date().getHours() < 18 ? 'afternoon' : 'evening' %>, 
                                <%= user.title %> <%= user.firstName %>! 👋
                            </h1>
                            <p class="lead mb-4 opacity-90">
                                Welcome back to your DUSW dashboard. You have <strong><%= notifications.length %></strong> active patients 
                                and <strong>3</strong> new notifications waiting for your attention.
                            </p>
                            <div class="d-flex gap-3 mb-4">
                                <div class="d-flex align-items-center text-white-50">
                                    <i class="fas fa-hospital me-2"></i>
                                    <small><%= user.dialysisClinic %></small>
                                </div>
                                <div class="d-flex align-items-center text-white-50">
                                    <i class="fas fa-envelope me-2"></i>
                                    <small><%= user.email %></small>
                                </div>
                                <div class="d-flex align-items-center text-white-50">
                                    <i class="fas fa-clock me-2"></i>
                                    <small>Last login: <span id="currentTime"></span></small>
                                </div>
                            </div>
                            <div class="d-flex flex-wrap gap-2">
                                <button class="btn btn-success btn-sm fw-semibold" data-bs-toggle="modal" data-bs-target="#referralModal">
                                    <i class="fas fa-user-plus me-2"></i>Refer New Patient
                                </button>
                                <a href="/patients" class="btn btn-light btn-sm fw-semibold">
                                    <i class="fas fa-users me-2"></i>View All Patients
                                </a>
                                <a href="/reports" class="btn btn-outline-light btn-sm">
                                    <i class="fas fa-chart-bar me-2"></i>Generate Report
                                </a>
                            </div>
                        </div>
                        <div class="col-lg-4 text-end">
                            <div class="position-relative">
                                <div class="bg-white bg-opacity-20 rounded-circle d-inline-flex align-items-center justify-content-center" 
                                     style="width: 120px; height: 120px;">
                                    <i class="fas fa-user-md" style="font-size: 3rem; color: white;"></i>
                                </div>
                                <div class="position-absolute top-0 end-0 bg-success rounded-circle d-flex align-items-center justify-content-center"
                                     style="width: 40px; height: 40px;">
                                    <i class="fas fa-check text-white" style="font-size: 1.2rem;"></i>
                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- Decorative elements -->
                    <div class="position-absolute" style="top: -50px; right: -50px; opacity: 0.1;">
                        <i class="fas fa-heartbeat" style="font-size: 200px; color: white;"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Modern Statistics Grid -->
    <div class="row mb-5">
        <!-- Total Patients Card -->
        <div class="col-xl-3 col-lg-6 col-md-6 mb-4">
            <div class="card border-0 h-100" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
                <div class="card-body text-white p-4">
                    <div class="d-flex justify-content-between align-items-start mb-3">
                        <div>
                            <div class="bg-white bg-opacity-20 rounded-circle d-inline-flex align-items-center justify-content-center mb-3"
                                 style="width: 50px; height: 50px;">
                                <i class="fas fa-users text-white"></i>
                            </div>
                            <h6 class="text-white text-uppercase fw-semibold mb-1" style="font-size: 0.75rem; letter-spacing: 1px;">
                                Total Patients
                            </h6>
                            <h2 class="fw-bold mb-0"><%= notifications.length %></h2>
                        </div>
                        <div class="text-end">
                            <span class="badge bg-white bg-opacity-90 text-dark">
                                <i class="fas fa-arrow-up me-1"></i>+12%
                            </span>
                        </div>
                    </div>
                    <div class="progress bg-white bg-opacity-20" style="height: 4px;">
                        <div class="progress-bar bg-white" role="progressbar" style="width: 75%"></div>
                    </div>
                    <small class="text-white mt-2 d-block">vs. last month</small>
                </div>
            </div>
        </div>

        <!-- Active Referrals Card -->
        <div class="col-xl-3 col-lg-6 col-md-6 mb-4">
            <div class="card border-0 h-100" style="background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);">
                <div class="card-body text-white p-4">
                    <div class="d-flex justify-content-between align-items-start mb-3">
                        <div>
                            <div class="bg-white bg-opacity-20 rounded-circle d-inline-flex align-items-center justify-content-center mb-3"
                                 style="width: 50px; height: 50px;">
                                <i class="fas fa-paper-plane text-white"></i>
                            </div>
                            <h6 class="text-white text-uppercase fw-semibold mb-1" style="font-size: 0.75rem; letter-spacing: 1px;">
                                Active Referrals
                            </h6>
                            <h2 class="fw-bold mb-0">
                                <%= notifications.reduce((sum, patient) => sum + parseInt(patient.referral_count || 0), 0) %>
                            </h2>
                        </div>
                        <div class="text-end">
                            <span class="badge bg-white bg-opacity-90 text-dark">
                                <i class="fas fa-arrow-up me-1"></i>+8%
                            </span>
                        </div>
                    </div>
                    <div class="progress bg-white bg-opacity-20" style="height: 4px;">
                        <div class="progress-bar bg-white" role="progressbar" style="width: 60%"></div>
                    </div>
                    <small class="text-white mt-2 d-block">vs. last month</small>
                </div>
            </div>
        </div>

        <!-- This Week Card -->
        <div class="col-xl-3 col-lg-6 col-md-6 mb-4">
            <div class="card border-0 h-100" style="background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);">
                <div class="card-body p-4">
                    <div class="d-flex justify-content-between align-items-start mb-3">
                        <div>
                            <div class="bg-white bg-opacity-40 rounded-circle d-inline-flex align-items-center justify-content-center mb-3"
                                 style="width: 50px; height: 50px;">
                                <i class="fas fa-calendar-week" style="color: #111827;"></i>
                            </div>
                            <h6 class="text-uppercase fw-semibold mb-1" style="font-size: 0.75rem; letter-spacing: 1px; color: #1f2937;">
                                This Week
                            </h6>
                            <h2 class="fw-bold mb-0" style="color: #111827;">
                                <%= notifications.filter(p => {
                                    const weekAgo = new Date();
                                    weekAgo.setDate(weekAgo.getDate() - 7);
                                    return new Date(p.created_at) >= weekAgo;
                                }).length %>
                            </h2>
                        </div>
                        <div class="text-end">
                            <span class="badge bg-success text-white">
                                <i class="fas fa-arrow-up me-1"></i>+15%
                            </span>
                        </div>
                    </div>
                    <div class="progress bg-white bg-opacity-40" style="height: 4px;">
                        <div class="progress-bar bg-success" role="progressbar" style="width: 90%"></div>
                    </div>
                    <small class="mt-2 d-block" style="color: #1f2937;">New registrations</small>
                </div>
            </div>
        </div>

        <!-- Pending Actions Card -->
        <div class="col-xl-3 col-lg-6 col-md-6 mb-4">
            <div class="card border-0 h-100" style="background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%);">
                <div class="card-body p-4">
                    <div class="d-flex justify-content-between align-items-start mb-3">
                        <div>
                            <div class="bg-white bg-opacity-40 rounded-circle d-inline-flex align-items-center justify-content-center mb-3"
                                 style="width: 50px; height: 50px;">
                                <i class="fas fa-exclamation-triangle" style="color: #78350f;"></i>
                            </div>
                            <h6 class="text-uppercase fw-semibold mb-1" style="font-size: 0.75rem; letter-spacing: 1px; color: #451a03;">
                                Needs Attention
                            </h6>
                            <h2 class="fw-bold mb-0" style="color: #78350f;">7</h2>
                        </div>
                        <div class="text-end">
                            <span class="badge bg-warning text-dark">
                                <i class="fas fa-bell"></i> Urgent
                            </span>
                        </div>
                    </div>
                    <div class="progress bg-white bg-opacity-40" style="height: 4px;">
                        <div class="progress-bar bg-warning" role="progressbar" style="width: 45%"></div>
                    </div>
                    <small class="mt-2 d-block" style="color: #451a03;">Require follow-up</small>
                </div>
            </div>
        </div>
    </div>

    <!-- Quick Actions Section -->
    <div class="row mb-5">
        <div class="col-12">
            <h4 class="fw-bold mb-4">
                <i class="fas fa-bolt me-2" style="color: var(--primary-color);"></i>
                Quick Actions
            </h4>
        </div>
        <div class="col-lg-3 col-md-6 mb-3">
            <div class="card border-0 h-100 quick-action-card" style="cursor: pointer;">
                <div class="card-body text-center p-4">
                    <div class="feature-icon mx-auto mb-3" style="background: var(--success-gradient);">
                        <i class="fas fa-user-plus"></i>
                    </div>
                    <h6 class="fw-semibold">Add New Patient</h6>
                    <p class="text-muted small mb-0">Register a new patient in the system</p>
                </div>
            </div>
        </div>
        <div class="col-lg-3 col-md-6 mb-3">
            <div class="card border-0 h-100 quick-action-card" style="cursor: pointer;">
                <div class="card-body text-center p-4">
                    <div class="feature-icon mx-auto mb-3" style="background: var(--secondary-gradient);">
                        <i class="fas fa-file-medical"></i>
                    </div>
                    <h6 class="fw-semibold">Create Report</h6>
                    <p class="text-muted small mb-0">Generate monthly progress reports</p>
                </div>
            </div>
        </div>
        <div class="col-lg-3 col-md-6 mb-3">
            <div class="card border-0 h-100 quick-action-card" style="cursor: pointer;">
                <div class="card-body text-center p-4">
                    <div class="feature-icon mx-auto mb-3" style="background: var(--warning-gradient);">
                        <i class="fas fa-calendar-check"></i>
                    </div>
                    <h6 class="fw-semibold">Schedule Follow-up</h6>
                    <p class="text-muted small mb-0">Book appointments with patients</p>
                </div>
            </div>
        </div>
        <div class="col-lg-3 col-md-6 mb-3">
            <div class="card border-0 h-100 quick-action-card" style="cursor: pointer;">
                <div class="card-body text-center p-4">
                    <div class="feature-icon mx-auto mb-3" style="background: var(--danger-gradient);">
                        <i class="fas fa-bell"></i>
                    </div>
                    <h6 class="fw-semibold">Send Notification</h6>
                    <p class="text-muted small mb-0">Notify patients about updates</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Main Content Tabs -->
    <div class="row">
        <div class="col-12">
            <div class="card border-0">
                <div class="card-header bg-transparent border-0 px-0">
                    <ul class="nav nav-pills nav-fill" id="dashboardTabs" role="tablist" style="gap: 0.5rem;">
                        <li class="nav-item">
                            <button class="nav-link active fw-semibold" id="notifications-tab" data-bs-toggle="tab" 
                                    data-bs-target="#notifications" type="button" role="tab"
                                    style="border-radius: var(--radius-md); border: 2px solid transparent;">
                                <i class="fas fa-users me-2"></i>Patient Activity
                                <% if (notifications.length > 0) { %>
                                    <span class="badge bg-light text-primary ms-2"><%= notifications.length %></span>
                                <% } %>
                            </button>
                        </li>
                        <li class="nav-item">
                            <button class="nav-link fw-semibold" id="alerts-tab" data-bs-toggle="tab" 
                                    data-bs-target="#alerts" type="button" role="tab"
                                    style="border-radius: var(--radius-md); border: 2px solid transparent;">
                                <i class="fas fa-bell me-2"></i>Notifications
                                <% if (typeof unreadNotificationCount !== 'undefined' && unreadNotificationCount > 0) { %>
                                    <span class="badge bg-danger ms-2"><%= unreadNotificationCount %></span>
                                <% } %>
                            </button>
                        </li>
                        <li class="nav-item">
                            <button class="nav-link fw-semibold" id="recent-tab" data-bs-toggle="tab" 
                                    data-bs-target="#recent" type="button" role="tab"
                                    style="border-radius: var(--radius-md); border: 2px solid transparent;">
                                <i class="fas fa-activity me-2"></i>Recent Actions
                            </button>
                        </li>
                        <li class="nav-item">
                            <button class="nav-link fw-semibold" id="analytics-tab" data-bs-toggle="tab" 
                                    data-bs-target="#analytics" type="button" role="tab"
                                    style="border-radius: var(--radius-md); border: 2px solid transparent;">
                                <i class="fas fa-chart-line me-2"></i>Analytics
                            </button>
                        </li>
                        <li class="nav-item">
                            <button class="nav-link fw-semibold" id="tools-tab" data-bs-toggle="tab" 
                                    data-bs-target="#tools" type="button" role="tab"
                                    style="border-radius: var(--radius-md); border: 2px solid transparent;">
                                <i class="fas fa-cogs me-2"></i>Management Tools
                            </button>
                        </li>
                    </ul>
                </div>
                
                <div class="card-body">
                    <div class="tab-content" id="dashboardTabContent">
                        <!-- Patient Notifications Tab -->
                        <div class="tab-pane fade show active" id="notifications" role="tabpanel">
                            <% if (notifications.length === 0) { %>
                                <!-- Empty State -->
                                <div class="text-center py-5">
                                    <div class="mb-4">
                                        <div class="feature-icon mx-auto mb-3" style="background: var(--secondary-gradient); opacity: 0.7;">
                                            <i class="fas fa-users"></i>
                                        </div>
                                        <h4 class="fw-bold mb-2">No Patient Activity Yet</h4>
                                        <p class="text-muted lead">
                                            Patient activity will appear here when patients from your dialysis clinic<br>
                                            register and select their transplant centers through the mobile app.
                                        </p>
                                    </div>
                                    <div class="d-flex justify-content-center gap-3">
                                        <a href="/patients" class="btn btn-primary">
                                            <i class="fas fa-users me-2"></i>Manage Patients
                                        </a>
                                        <button class="btn btn-outline-primary">
                                            <i class="fas fa-mobile-alt me-2"></i>Mobile App Guide
                                        </button>
                                    </div>
                                </div>
                            <% } else { %>
                                <!-- Patients Grid -->
                                <div class="row g-4">
                                    <% notifications.forEach((patient, index) => { %>
                                        <div class="col-xl-6 col-lg-6 col-md-12">
                                            <div class="card border-0 patient-card h-100" style="background: linear-gradient(135deg, #1e293b 0%, #334155 100%); transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);">
                                                <div class="card-body p-4">
                                                    <!-- Patient Header -->
                                                    <div class="d-flex align-items-start mb-4">
                                                        <div class="flex-shrink-0 me-3">
                                                            <div class="rounded-circle d-flex align-items-center justify-content-center position-relative" 
                                                                 style="width: 60px; height: 60px; background: <%= ['var(--primary-gradient)', 'var(--success-gradient)', 'var(--secondary-gradient)', 'var(--warning-gradient)'][index % 4] %>;">
                                                                <i class="fas fa-user text-white" style="font-size: 1.5rem;"></i>
                                                                <% if (patient.referral_count > 0) { %>
                                                                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-success" style="font-size: 0.6rem;">
                                                                        <i class="fas fa-check"></i>
                                                                    </span>
                                                                <% } %>
                                                            </div>
                                                        </div>
                                                        <div class="flex-grow-1">
                                                            <h6 class="text-slate-50 fw-semibold mb-1 text-lg" style="color: #f8fafc !important; font-size: 1.125rem; line-height: 1.4;">
                                                                <%= patient.first_name %> <%= patient.last_name %>
                                                            </h6>
                                                            <p class="text-slate-300 mb-2" style="font-size: 0.9rem; color: #cbd5e1 !important;">
                                                                <i class="fas fa-envelope me-2" style="color: #94a3b8;"></i><%= patient.email %>
                                                            </p>
                                                            <div class="d-flex align-items-center gap-3">
                                                                <small class="text-slate-400" style="color: #94a3b8 !important;">
                                                                    <i class="fas fa-calendar-day me-1" style="color: #94a3b8;"></i>
                                                                    Joined <%= new Date(patient.created_at).toLocaleDateString('en-US', { month: 'short', day: 'numeric' }) %>
                                                                </small>
                                                                <% if (new Date() - new Date(patient.created_at) < 7 * 24 * 60 * 60 * 1000) { %>
                                                                    <span class="badge bg-primary bg-opacity-10 text-primary">New</span>
                                                                <% } %>
                                                            </div>
                                                        </div>
                                                    </div>
                                                    
                                                    <!-- Status & Progress -->
                                                    <div class="mb-4">
                                                        <% if (patient.referral_count > 0) { %>
                                                            <div class="status-badge status-completed mb-3">
                                                                <i class="fas fa-hospital"></i>
                                                                <%= patient.referral_count %> Transplant Center<%= patient.referral_count > 1 ? 's' : '' %> Selected
                                                            </div>
                                                            <div class="progress bg-success bg-opacity-20" style="height: 6px;">
                                                                <div class="progress-bar bg-success" role="progressbar" style="width: 100%"></div>
                                                            </div>
                                                            <small class="text-success fw-semibold mt-2 d-block">
                                                                <i class="fas fa-check-circle me-1"></i>Onboarding Complete
                                                            </small>
                                                        <% } else { %>
                                                            <div class="status-badge status-pending mb-3">
                                                                <i class="fas fa-clock"></i>
                                                                Setting up profile...
                                                            </div>
                                                            <div class="progress bg-warning bg-opacity-20" style="height: 6px;">
                                                                <div class="progress-bar bg-warning" role="progressbar" style="width: 40%"></div>
                                                            </div>
                                                            <small class="text-warning fw-semibold mt-2 d-block">
                                                                <i class="fas fa-hourglass-half me-1"></i>Awaiting Transplant Center Selection
                                                            </small>
                                                        <% } %>
                                                    </div>
                                                    
                                                    <!-- Action Buttons -->
                                                    <div class="d-flex gap-2">
                                                        <a href="/patients/<%= patient.patient_id %>" class="btn btn-primary btn-sm flex-fill">
                                                            <i class="fas fa-eye me-2"></i>View Details
                                                        </a>
                                                        <button class="btn btn-outline-success btn-sm flex-fill" onclick="contactPatient('<%= patient.email %>')">
                                                            <i class="fas fa-comments me-2"></i>Contact
                                                        </button>
                                                        <div class="dropdown">
                                                            <button class="btn btn-outline-secondary btn-sm dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                                                <i class="fas fa-ellipsis-v"></i>
                                                            </button>
                                                            <ul class="dropdown-menu">
                                                                <li><a class="dropdown-item" href="#"><i class="fas fa-notes-medical me-2"></i>Add Note</a></li>
                                                                <li><a class="dropdown-item" href="#"><i class="fas fa-calendar me-2"></i>Schedule Follow-up</a></li>
                                                                <li><hr class="dropdown-divider"></li>
                                                                <li><a class="dropdown-item text-danger" href="#"><i class="fas fa-flag me-2"></i>Flag Patient</a></li>
                                                            </ul>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    <% }); %>
                                </div>
                                
                                <!-- View All Button -->
                                <div class="text-center mt-5">
                                    <a href="/patients" class="btn btn-outline-primary btn-lg">
                                        <i class="fas fa-users me-2"></i>View All Patients
                                        <span class="badge bg-primary ms-2"><%= notifications.length %></span>
                                    </a>
                                </div>
                            <% } %>
                        </div>
                        
                        <!-- Notifications/Alerts Tab -->
                        <div class="tab-pane fade" id="alerts" role="tabpanel">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <h5 class="mb-0">
                                    <i class="fas fa-bell text-warning me-2"></i>
                                    Patient Notifications
                                </h5>
                                <% if (typeof duswNotifications !== 'undefined' && duswNotifications.length > 0) { %>
                                    <button class="btn btn-outline-primary btn-sm" onclick="markAllRead()">
                                        <i class="fas fa-check-double me-1"></i>Mark All as Read
                                    </button>
                                <% } %>
                            </div>
                            
                            <% if (typeof duswNotifications === 'undefined' || duswNotifications.length === 0) { %>
                                <div class="text-center py-5">
                                    <div class="feature-icon mx-auto mb-3" style="background: var(--secondary-gradient); opacity: 0.7;">
                                        <i class="fas fa-bell-slash"></i>
                                    </div>
                                    <h4 class="fw-bold mb-2">No Notifications</h4>
                                    <p class="text-muted">You'll receive notifications when your patients upload documents or complete their intake forms.</p>
                                </div>
                            <% } else { %>
                                <div class="list-group">
                                    <% duswNotifications.forEach(notification => { %>
                                        <div class="list-group-item list-group-item-action <%= notification.is_read ? '' : 'bg-light border-start border-4 border-primary' %>" 
                                             onclick="markAsRead('<%= notification.id %>')" style="cursor: pointer;">
                                            <div class="d-flex w-100 justify-content-between align-items-start">
                                                <div>
                                                    <div class="d-flex align-items-center mb-1">
                                                        <% if (notification.notification_type === 'documents_complete') { %>
                                                            <span class="badge bg-success me-2"><i class="fas fa-file-alt me-1"></i>Documents</span>
                                                        <% } else if (notification.notification_type === 'intake_form_complete') { %>
                                                            <span class="badge bg-info me-2"><i class="fas fa-clipboard-check me-1"></i>Intake Form</span>
                                                        <% } else { %>
                                                            <span class="badge bg-secondary me-2"><i class="fas fa-info me-1"></i>Update</span>
                                                        <% } %>
                                                        <h6 class="mb-0 <%= notification.is_read ? 'text-muted' : 'fw-bold' %>"><%= notification.title %></h6>
                                                    </div>
                                                    <p class="mb-1 text-muted"><%= notification.message %></p>
                                                    <% if (notification.patient_first_name) { %>
                                                        <small class="text-primary">
                                                            <i class="fas fa-user me-1"></i><%= notification.patient_first_name %> <%= notification.patient_last_name %>
                                                        </small>
                                                    <% } %>
                                                </div>
                                                <div class="text-end">
                                                    <small class="text-muted"><%= new Date(notification.created_at).toLocaleDateString() %></small>
                                                    <br>
                                                    <small class="text-muted"><%= new Date(notification.created_at).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'}) %></small>
                                                    <% if (!notification.is_read) { %>
                                                        <br><span class="badge bg-primary mt-1">New</span>
                                                    <% } %>
                                                </div>
                                            </div>
                                        </div>
                                    <% }); %>
                                </div>
                            <% } %>
                        </div>
                        
                        <!-- Recent Activity Tab -->
                        <div class="tab-pane fade" id="recent" role="tabpanel">
                            <h5 class="mb-3">
                                <i class="fas fa-clock text-info me-2"></i>
                                Recent Activity
                            </h5>
                            <div class="list-group">
                                <div class="list-group-item">
                                    <div class="d-flex w-100 justify-content-between">
                                        <h6 class="mb-1">System Login</h6>
                                        <small>Just now</small>
                                    </div>
                                    <p class="mb-1">Successful login from your account</p>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Reports Tab -->
                        <div class="tab-pane fade" id="reports" role="tabpanel">
                            <h5 class="mb-3">
                                <i class="fas fa-chart-bar text-success me-2"></i>
                                Reports & Analytics
                            </h5>
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="card">
                                        <div class="card-body text-center">
                                            <i class="fas fa-download fa-2x text-primary mb-3"></i>
                                            <h6>Monthly Patient Report</h6>
                                            <p class="text-muted small">Export patient activity data</p>
                                            <button class="btn btn-outline-primary btn-sm">Download</button>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="card">
                                        <div class="card-body text-center">
                                            <i class="fas fa-chart-pie fa-2x text-info mb-3"></i>
                                            <h6>Referral Analytics</h6>
                                            <p class="text-muted small">View referral success rates</p>
                                            <button class="btn btn-outline-info btn-sm">View Report</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Tools Tab -->
                        <div class="tab-pane fade" id="tools" role="tabpanel">
                            <h5 class="mb-3">
                                <i class="fas fa-tools text-warning me-2"></i>
                                DUSW Tools
                            </h5>
                            <div class="row">
                                <div class="col-md-4">
                                    <div class="card">
                                        <div class="card-body text-center">
                                            <i class="fas fa-users fa-2x text-primary mb-3"></i>
                                            <h6>Patient Manager</h6>
                                            <p class="text-muted small">Manage all your patients</p>
                                            <a href="/patients" class="btn btn-outline-primary btn-sm">Open</a>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="card">
                                        <div class="card-body text-center">
                                            <i class="fas fa-hospital fa-2x text-success mb-3"></i>
                                            <h6>Center Directory</h6>
                                            <p class="text-muted small">Browse transplant centers</p>
                                            <button class="btn btn-outline-success btn-sm">Browse</button>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="card">
                                        <div class="card-body text-center">
                                            <i class="fas fa-cog fa-2x text-secondary mb-3"></i>
                                            <h6>Settings</h6>
                                            <p class="text-muted small">Account preferences</p>
                                            <button class="btn btn-outline-secondary btn-sm">Configure</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Referral Modal -->
    <div class="modal fade" id="referralModal" tabindex="-1" aria-labelledby="referralModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
            <div class="modal-content border-0 shadow-lg">
                <div class="modal-header bg-gradient border-0" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
                    <div>
                        <h5 class="modal-title text-white fw-bold mb-0" id="referralModalLabel">
                            <i class="fas fa-user-plus me-2"></i>Refer a New Patient
                        </h5>
                        <small class="text-white-50">Invite a patient to join Transplant Wizard</small>
                    </div>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body p-4">
                    <form id="referralForm">
                        <!-- Patient Information Section -->
                        <div class="mb-4">
                            <h5 class="fw-bold mb-3" style="color: #1f2937; font-size: 1.1rem;">
                                Patient Information
                            </h5>

                            <!-- Title -->
                            <div class="mb-3">
                                <label for="patientTitle" class="form-label fw-bold" style="color: #374151; font-size: 0.95rem;">Title (Optional)</label>
                                <select class="form-select form-select-sm" id="patientTitle" name="patientTitle" style="background-color: #f3f4f6; border-color: #d1d5db;">
                                    <option value="">Select title...</option>
                                    <option value="Mr">Mr</option>
                                    <option value="Mrs">Mrs</option>
                                    <option value="Ms">Ms</option>
                                    <option value="Dr">Dr</option>
                                    <option value="Prof">Prof</option>
                                </select>
                            </div>

                            <!-- First Name -->
                            <div class="mb-3">
                                <label for="patientFirstName" class="form-label fw-bold" style="color: #374151; font-size: 0.95rem;">First Name <span class="text-danger">*</span></label>
                                <input type="text" class="form-control form-control-sm" id="patientFirstName" name="patientFirstName" placeholder="John" required style="background-color: #f3f4f6; border-color: #d1d5db; color: #111827;">
                            </div>

                            <!-- Last Name -->
                            <div class="mb-3">
                                <label for="patientLastName" class="form-label fw-bold" style="color: #374151; font-size: 0.95rem;">Last Name <span class="text-danger">*</span></label>
                                <input type="text" class="form-control form-control-sm" id="patientLastName" name="patientLastName" placeholder="Doe" required style="background-color: #f3f4f6; border-color: #d1d5db; color: #111827;">
                            </div>

                            <!-- Email -->
                            <div class="mb-3">
                                <label for="patientEmail" class="form-label fw-bold" style="color: #374151; font-size: 0.95rem;">Email Address <span class="text-danger">*</span></label>
                                <input type="email" class="form-control form-control-sm" id="patientEmail" name="patientEmail" placeholder="john.doe@example.com" required style="background-color: #f3f4f6; border-color: #d1d5db; color: #111827;">
                            </div>

                            <!-- Nephrologist -->
                            <div class="mb-3">
                                <label for="patientNephrologist" class="form-label fw-bold" style="color: #374151; font-size: 0.95rem;">Nephrologist (Optional)</label>
                                <input type="text" class="form-control form-control-sm" id="patientNephrologist" name="patientNephrologist" placeholder="Dr. Jane Smith" style="background-color: #f3f4f6; border-color: #d1d5db; color: #111827;">
                            </div>
                        </div>

                        <!-- Clinic Information Section -->
                        <div class="mb-4">
                            <h5 class="fw-bold mb-3" style="color: #1f2937; font-size: 1.1rem;">
                                Clinic Information
                            </h5>

                            <!-- Dialysis Clinic (Auto-filled) -->
                            <div class="mb-3">
                                <label for="dialysisClinic" class="form-label fw-bold" style="color: #374151; font-size: 0.95rem;">Dialysis Clinic</label>
                                <input type="text" class="form-control form-control-sm" id="dialysisClinic" name="dialysisClinic" value="<%= user.dialysisClinic %>" readonly style="background-color: #f3f4f6; border-color: #d1d5db; color: #111827;">
                            </div>

                            <!-- Dialysis Clinic ID (Hidden) -->
                            <input type="hidden" id="dialysisClinicId" name="dialysisClinicId" value="<%= user.dialysisClinicId || 'default-clinic-id' %>">
                        </div>

                        <!-- DUSW Information Section (Hidden) -->
                        <input type="hidden" id="duswId" name="duswId" value="<%= user.id %>">
                        <input type="hidden" id="duswEmail" name="duswEmail" value="<%= user.email %>">
                        <input type="hidden" id="duswName" name="duswName" value="<%= user.title %> <%= user.firstName %> <%= user.lastName %>">

                        <!-- Form Alerts -->
                        <div id="successAlert" class="alert alert-success alert-dismissible fade show d-none" role="alert" style="border-left: 4px solid #28a745; background-color: #d4edda;">
                            <div class="mb-2">
                                <i class="fas fa-check-circle me-2" style="color: #28a745; font-size: 1.2rem;"></i>
                                <strong style="font-size: 1.1rem; color: #155724;">Referral Sent Successfully!</strong>
                            </div>
                            <p class="mb-2" style="color: #155724; font-size: 0.95rem;">
                                Patient referral has been created for:
                                <br><strong id="successPatientName"></strong>
                                <br><em id="successPatientEmail"></em>
                            </p>
                            <p class="mb-0" style="color: #155724; font-size: 0.9rem;">
                                An invitation email has been sent to the patient's email address with a link to register on Transplant Wizard.
                            </p>
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>

                        <div id="errorAlert" class="alert alert-danger alert-dismissible fade show d-none" role="alert">
                            <i class="fas fa-exclamation-circle me-2"></i>
                            <strong>Error:</strong> <span id="errorMessage"></span>
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    </form>
                </div>
                <div class="modal-footer border-0 bg-light">
                    <button type="button" class="btn btn-secondary btn-sm" data-bs-dismiss="modal">Cancel</button>
                    <button type="button" class="btn btn-primary btn-sm fw-semibold" id="submitReferral">
                        <i class="fas fa-paper-plane me-2"></i><span id="submitText">Send Referral</span>
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Success Modal for Referral -->
    <div class="modal fade" id="successModal" tabindex="-1" aria-labelledby="successModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content border-0 shadow-lg" style="border-radius: 16px; overflow: hidden;">
                <div class="modal-body text-center p-5">
                    <div class="mb-4">
                        <div style="width: 80px; height: 80px; background: linear-gradient(135deg, #28a745 0%, #20c997 100%); border-radius: 50%; display: inline-flex; align-items: center; justify-content: center; box-shadow: 0 8px 24px rgba(40, 167, 69, 0.3);">
                            <i class="fas fa-check" style="font-size: 36px; color: white;"></i>
                        </div>
                    </div>
                    <h4 class="fw-bold mb-3" style="color: #1a1a2e;">Referral Sent Successfully!</h4>
                    <p class="text-muted mb-2">Patient referral has been created for:</p>
                    <p class="fw-semibold mb-1" style="color: #1a1a2e; font-size: 1.1rem;" id="successModalPatientName"></p>
                    <p class="text-muted mb-4" style="font-size: 0.95rem;" id="successModalPatientEmail"></p>
                    <div class="alert alert-light border-0 mb-4" style="background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%); border-radius: 12px;">
                        <i class="fas fa-envelope me-2" style="color: #6c757d;"></i>
                        <span style="color: #495057; font-size: 0.9rem;">An invitation email has been sent to the patient.</span>
                    </div>
                    <button type="button" class="btn btn-primary px-5 py-2 fw-semibold" id="successDoneBtn" style="border-radius: 8px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border: none; box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);">
                        <i class="fas fa-check me-2"></i>Done
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- JavaScript for Referral Form -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            console.log('🔍 Dashboard script loaded - initializing referral form');

            const referralForm = document.getElementById('referralForm');
            const submitButton = document.getElementById('submitReferral');
            const successAlert = document.getElementById('successAlert');
            const errorAlert = document.getElementById('errorAlert');
            const errorMessage = document.getElementById('errorMessage');
            const submitText = document.getElementById('submitText');

            if (!submitButton) {
                console.error('❌ Submit button not found!');
                return;
            }

            console.log('✅ Form elements found, attaching click handler');

            submitButton.addEventListener('click', async function(e) {
                e.preventDefault();
                console.log('🔗 Submit button clicked');

                // Clear previous alerts
                successAlert.classList.add('d-none');
                errorAlert.classList.add('d-none');

                // Validate form
                if (!referralForm.checkValidity()) {
                    console.log('⚠️ Form validation failed');
                    referralForm.reportValidity();
                    return;
                }

                console.log('✅ Form validation passed');

                // Disable button and show loading state
                submitButton.disabled = true;
                const originalText = submitText.textContent;
                submitText.textContent = 'Sending...';
                submitButton.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span><span id="submitText">Sending...</span>';

                try {
                    // Prepare form data
                    const formData = {
                        patientTitle: document.getElementById('patientTitle').value || null,
                        patientFirstName: document.getElementById('patientFirstName').value,
                        patientLastName: document.getElementById('patientLastName').value,
                        patientEmail: document.getElementById('patientEmail').value,
                        patientNephrologist: document.getElementById('patientNephrologist').value || null,
                        dialysisClinicId: document.getElementById('dialysisClinicId').value,
                        dialysisClinicName: document.getElementById('dialysisClinic').value,
                        duswId: document.getElementById('duswId').value,
                        duswEmail: document.getElementById('duswEmail').value,
                        duswName: document.getElementById('duswName').value
                    };

                    console.log('📤 Sending referral data:', formData);

                    // Make API request to backend API
                    const response = await fetch('https://api.transplantwizard.com/api/v1/dusw/referrals/create', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify(formData)
                    });

                    console.log('📨 API response status:', response.status);

                    const data = await response.json();
                    console.log('📦 API response data:', data);

                    if (!response.ok || data.success === false) {
                        throw new Error(data.error || 'Failed to send referral');
                    }

                    console.log('✅ Referral created successfully');

                    // Get patient info before resetting form
                    const patientName = document.getElementById('patientFirstName').value + ' ' + document.getElementById('patientLastName').value;
                    const patientEmail = document.getElementById('patientEmail').value;

                    // Close referral modal
                    const referralModal = bootstrap.Modal.getInstance(document.getElementById('referralModal'));
                    if (referralModal) {
                        referralModal.hide();
                    }

                    // Update success modal with patient info
                    document.getElementById('successModalPatientName').textContent = patientName;
                    document.getElementById('successModalPatientEmail').textContent = patientEmail;

                    // Show success modal
                    const successModal = new bootstrap.Modal(document.getElementById('successModal'));
                    successModal.show();

                    // Reset form
                    referralForm.reset();

                } catch (error) {
                    console.error('❌ Referral error:', error);
                    errorMessage.textContent = error.message || 'An unexpected error occurred. Please try again.';
                    errorAlert.classList.remove('d-none');
                } finally {
                    // Re-enable button
                    submitButton.disabled = false;
                    submitText.textContent = originalText;
                    submitButton.innerHTML = '<i class="fas fa-paper-plane me-2"></i><span id="submitText">Send Referral</span>';
                }
            });

            // Reset alerts when modal closes
            document.getElementById('referralModal').addEventListener('hidden.bs.modal', function() {
                successAlert.classList.add('d-none');
                errorAlert.classList.add('d-none');
            });

            console.log('✅ Referral form initialization complete');

            // Attach event listener to success Done button
            document.getElementById('successDoneBtn').addEventListener('click', function() {
                const successModal = bootstrap.Modal.getInstance(document.getElementById('successModal'));
                if (successModal) {
                    successModal.hide();
                }
                window.location.reload();
            });
        });

        // Notification functions
        async function markAsRead(notificationId) {
            try {
                await fetch(`/notifications/${notificationId}/read`, { method: 'POST' });
                // Refresh page to update notification count
                location.reload();
            } catch (error) {
                console.error('Error marking notification as read:', error);
            }
        }

        async function markAllRead() {
            try {
                await fetch('/notifications/mark-all-read', { method: 'POST' });
                // Refresh page to update notification count
                location.reload();
            } catch (error) {
                console.error('Error marking all notifications as read:', error);
            }
        }
    </script>
</div>

<%- include('partials/footer') %>